import { DefaultLayout } from 'layouts';
import React from 'react';

type Props = {};

const StatusProduct = (props: Props) => {
  return (
    <DefaultLayout title="Trạng thái sản phẩm" isSearch>
      <div>StatusUser</div>
    </DefaultLayout>
  );
};

export default StatusProduct;
