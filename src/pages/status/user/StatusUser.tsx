import { DefaultLayout } from 'layouts';
import React from 'react';

type Props = {};

const StatusUser = (props: Props) => {
  return (
    <DefaultLayout title="Trạng thái tài khoản" isSearch>
      <div>StatusUser</div>
    </DefaultLayout>
  );
};

export default StatusUser;
